TODO: Implementation in rundeck 

# execute preparation script
code_server_rundeck_hpc_user_prepare.sh

# local command
mkdir -p /home/rundeck/server/data/tmp

# local command
bash -c 'echo -n ${option.PassW} | openssl dgst -sha256 > /home/rundeck/server/data/tmp/code_trans.yml'

# do copy
/home/rundeck/server/data/tmp
/beegfs/${job.username}/code_server_playground${option.version}
code_trans.yml

# remove transfer file
rm /home/rundeck/server/data/tmp/code_trans.yml

# execute actual script
code_server_rundeck_hpc_user.sh

